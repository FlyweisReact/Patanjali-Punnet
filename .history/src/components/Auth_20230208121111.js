const token = localStorage.getItem("token")

export const Auth = () => {

 MdImportExport    const Auth = {
        headers : {
            Authorization : `Bearer ${token}`
        }
    }
     
  
}
